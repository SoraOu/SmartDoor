<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color (optional, e.g., white) -->
    <stroke
        android:color="#000000"
        android:width="10dp"/>
        <corners android:radius="8dp"/>
</shape>
